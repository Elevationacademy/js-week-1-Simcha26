//================Exercise_1======================
// Directions:
// Write a function called calcAge in such a way that when the code below executes, age is 28.

const calcAge = function (x, y) {
return x - y
}
calcAge()
const age = calcAge(2017, 1989)
console.log(`the age is ${age} years old`)
